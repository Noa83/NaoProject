{% extends 'base.html.twig' %}
{% block title %}Tableau de Bord{% endblock %}

{% block stylesheet_enfant %}
    <link rel="stylesheet" href="{{ asset('css/adminDashboard.css') }}">
{% endblock %}

{% block navi5 %}
    <li class="dropdown active">
{% endblock %}

{% block body %}

    <div class="container-fluid" id="adminDash_bg">
        <div class="col-md-offset-4 col-md-4">
        <h1 id="dash_title" class="text-center">Tableau de Bord</h1>
        </div>
        <br class="col-md-12">
        <div class="col-md-12">
            <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
                <ul class="nav nav-tabs nav-justified">
                    <li><a href="{{ path('admin_user') }}">Gestion des utilisateurs</a></li>
                    <li><a href="{{ path('admin_article') }}">Création/Modification articles</a></li>
                    <li><a href="{{ path('admin_rapport') }}">Rapports d'observation</a></li>
                </ul>
            </div>
        </div>
        <div class="col-md-12">
            {% for type, flash_messages in app.session.flashBag.all %}
                {% for flash_message in flash_messages %}
                    <div class="alert-{{ type }} text-center">
                        {{ flash_message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

        {% block adminContent %}
        {% endblock %}
    </div>


{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/jquery.ui.datepicker-fr.js') }}"></script>
    <script src="{{ asset('js/account.js') }}"></script>

{% endblock %}