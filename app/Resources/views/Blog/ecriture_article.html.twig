{% extends 'base.html.twig' %}

{% block stylesheet_enfant %}
    <link rel="stylesheet" href="{{ asset('css/ecriture_article.css') }}">
    <script src="https://cloud.tinymce.com/stable/tinymce.min.js?apiKey=kk0rqsx4ustkjlm70ut883a5hetalz9s5uitpohirak9393x"></script>
    <script>
        tinymce.init({
            selector: 'textarea',
            height: 500,
            menubar: false,
            plugins: [
                'advlist autolink lists link image charmap print preview anchor',
                'searchreplace visualblocks code fullscreen',
                'insertdatetime media table contextmenu paste code'
            ],
            toolbar: 'undo redo | insert | styleselect | bold italic | alignleft aligncenter alignright alignjustify | bullist numlist outdent indent | link image',
            content_css: '//www.tinymce.com/css/codepen.min.css'
        });
    </script>
{% endblock %}

{% block body %}
    <div class="container">

        <div class="row">
            <div class="col-lg-12">
                    <h1>Ecriture d'un article:</h1>
            <div>
        </div>
        <br>

        <div class="row">
            <div class="col-lg-12">
                {{ form_start(form) }}
                {{ form_errors(form) }}

                <div class="well">
                    <div class="form-group">
                        <label for="nom">Titre</label>
                        {{ form_widget(form.title, {'attr': {'class': 'form-control', 'placeholder': 'Titre'}})}}
                        {{ form_errors(form.title) }}
                    </div>

                    <div class="form-group">
                        <label for="date">Date</label>
                        {{ form_widget(form.date)}}
                        {{ form_errors(form.date) }}
                    </div>

                    <div class="form-group">
                        <label for="category">Categorie</label>
                        {{ form_widget(form.category)}}
                        {{ form_errors(form.category) }}
                    </div>

                    <div class="form-group">
                        <label for="picture">Image</label>
                        {{ form_widget(form.picture)}}
                        {{ form_errors(form.picture) }}
                    </div>

                    <div class="form-group">
                        <label for="contenu">Contenu</label>
                        {{ form_widget(form.text, {'attr': {'required': 'false'}})}}
                        {{ form_errors(form.text)}}
                    </div>

                    {{ form_widget(form.Valider, {'attr': {'class': 'btn btn-warning pull-right'}})}}
                </div>

                {{ form_rest(form) }}
                {{ form_end(form) }}
            </div>
        </div>

        <!--<form method="post">
            <textarea id="mytextarea">Hello, World!</textarea>
        </form>-->
    </div>
{% endblock %}

{% block JS %}
    <script>
        $(function(){
            $('#appbundle_article_text').removeAttr("required");
        });
    </script>
{% endblock %}

