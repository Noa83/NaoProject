{% extends 'base.html.twig' %}
{% block title %}Fiche Espèce{% endblock %}

{% block stylesheet_enfant %}
    <link rel="stylesheet" href="{{ asset('css/consultation.css') }}">
{% endblock %}

{% block navi3 %}
    <li class="active"><a href="{{ path('results') }}">Consultation<span class="sr-only">(current)</span></a></li>
{% endblock %}

{% block body %}
    <div class="container-fluid" id="consultation_bg">
        <div class="col-md-12">
            <h1 class="text-center">Consulter les données</h1>
            <div class="colonne_infos col-md-6 col-xs-12" id="bg_infos_consult">
                <div class="col-md-12">
                    <h3 class="text-center">Fiche Espèce :</h3><br/>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Regne de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.regne }}</p>
                    </div>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Phylum de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.phylum }}</p>
                    </div>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Classe de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.classe }}</p>
                    </div>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Ordre de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.ordre }}</p>
                    </div>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Famille de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.famille }}</p>
                    </div>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Nom Complet de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.nomComplet }}</p>
                    </div>
                    <div class="englob_fiche">
                        <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Nom Valide de l'oiseau :</p>
                        <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.nomValide }}</p>
                    </div>
                    {% if birdChoisi.nomVern is not empty %}
                        <div class="englob_fiche">
                            <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Nom Vern de l'oiseau :</p>
                            <p class="col-xs-7 col-sm-6 col-md-7"> {{ birdChoisi.nomVern }}</p>
                        </div>
                    {% endif %}
                </div>
                <div class="col-md-12">
                    <div class="col-xs-12">
                        <h3 class="text-center">Information sur l'observation :</h3><br/>
                        <div class="englob_fiche">
                            <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Commentaire :</p>
                            <p class="col-xs-7 col-sm-6 col-md-7"> {{ observation.comment }}</p>
                        </div>
                        <div class="englob_fiche">
                            <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Date :</p>
                            <p class="col-xs-7 col-sm-6 col-md-7"> {{ observation.date | date("d/m/Y") }}</p>
                        </div>
                        <div class="englob_fiche">
                            <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Maille :</p>
                            <p class="col-xs-7 col-sm-6 col-md-7"> {{ observation.km10Maille.getNomMaille() }}</p>
                        </div>
                        {% if observation.imageUrl %}
                            <div class="englob_fiche">
                                <p class="col-xs-5 col-sm-6 col-md-5 label_oiseau">Image :</p>
                                <p class="col-xs-7 col-sm-6 col-md-7"><a id="modal-declenche">Cliquez ici</a></p>
                            </div>
                            <div class="modal fade" id="enlargeImageModal" tabindex="-1" role="dialog"
                                 aria-labelledby="enlargeImageModal" aria-hidden="true">
                                <div class="modal-dialog modal-lg" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <button type="button" class="close" data-dismiss="modal"
                                                    aria-label="Close"><span aria-hidden="true">×</span>
                                            </button>
                                        </div>
                                        <div class="modal-body">
                                            <img src="{{ asset(observation.imageUrl) }}"
                                                 class="enlargeImageModalSource" style="width: 100%;">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% endif %}
                    </div>
                    <div class="text-center">
                        <a class="btn btn-default btn_more"
                           href="{{ path('results_list', {'birdId': birdChoisi.id}) }}">Retour à la liste</a>
                    </div>
                    <br>
                    <div class="col-md-10 col-md-offset-1">
                        <div>
                            <h3 class="text-center">Information sur l'observation :</h3><br/>
                            <p>Commentaire : {{ observation.comment }}</p>
                            <p>Date : {{ observation.date | date("d/m/Y") }}</p>
                            <p>Maille : {{ observation.km10Maille.getNomMaille() }}</p>
                            {% if observation.imageUrl %}
                                <p>Image :{{ asset(observation.imageUrl) }} <img src="{{ asset(observation.imageUrl) }}" alt="{{ observation.bird.getNomVern() }}"></p>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}
                </div>
            </div>
            <div class="colonne_carte col-md-6 col-xs-12">
                <div id="map" class="col-md-12"></div>
                <div class="col-md-12 bg_legend">
                    Les carrés colorés représentent les zones dans lesquelles l'espèce a été observée.<br>
                    Plus la couleur du carré est foncée plus un grand nombre d'individus y a été vu.<br>
                </div>
            </div>

            <script type="text/javascript">
                var map;
                function initMap() {
                    map = new google.maps.Map(document.getElementById('map'), {
                        center: {lat: {{ observation.lattitude }}, lng: {{ observation.longitude }}},
                        zoom: 8,
                        mapTypeId: 'terrain'
                    });
                    {% if birdChoisi is not empty %}
                    map.data.loadGeoJson('{{ path('fiche_json',{'id': observation.bird.id, 'obsId': observation.id}) }}');
                    map.data.setStyle({
                        fillColor: '#FE9A04',
                        fillOpacity: 0.5,
                        strokeWeight: 0
                    });
                    map.data.addListener('mouseover', function (event) {
                        map.data.revertStyle();
                        map.data.overrideStyle(event.feature, {strokeWeight: 1, strokeOpacity: 0.5});
                    });
                    {% endif %}
                }
            </script>
            <script async defer
                    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDHtk1Q7isnHjGK7ApFU2_V5--RdvqVUks&callback=initMap">
            </script>

        </div>
    </div>
{% endblock %}
{% block JS %}
    <script src="{{ asset('js/tooltip.js') }}"></script>
    <script src="{{ asset('js/modalImage.js') }}"></script>
{% endblock %}